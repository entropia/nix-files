[project]
name = "mqtt-publisher"
version = "1.0.0"

dependencies = [
  "paho-mqtt"
]

[project.scripts]
mqtt-publisher = "mqtt_publisher:main"

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"
